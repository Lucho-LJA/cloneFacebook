<%  if current_user.provider.present?%>
    <%= link_to image_tag(current_user.img_auth, class: class_), edit_user_registration_path %>
<% else %>
    <%= link_to image_tag(current_user.avatar.url, class: class_), edit_user_registration_path %>
<% end %>